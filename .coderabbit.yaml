# CodeRabbit configuration — AmakaFlow
language: "en-US"

reviews:
  profile: "assertive"
  auto_review:
    enabled: false  # CR PR #83 review fix: org policy = manual @coderabbitai trigger only
    drafts: false

knowledge_base:
  linear:
    usage: enabled
    team_keys:
      - "AMA"
  mcp:
    usage: enabled
  code_guidelines:
    enabled: true
    filePatterns:
      - "CLAUDE.md"
      - "ARCHITECTURE.md"
