name: Agent Validation Gate

on:
  push:
    branches: ['agent/**']

concurrency:
  group: agent-validation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build
        id: build
        timeout-minutes: 10
        run: ./gradlew assembleDebug

      - name: Run Unit Tests
        id: tests
        timeout-minutes: 10
        if: always() && steps.build.outcome == 'success'
        run: ./gradlew testDebugUnitTest

      - name: Run Lint
        id: lint
        timeout-minutes: 5
        if: always() && steps.build.outcome == 'success'
        run: ./gradlew lintDebug

      # ============================================
      # Maestro E2E Tests (AMA-622)
      # ============================================
      - name: Run Maestro E2E Smoke
        id: maestro
        timeout-minutes: 15
        if: always() && steps.build.outcome == 'success'
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          arch: x86_64
          target: google_apis
          profile: Nexus 6
          emulator-options: -no-window -no-audio -no-boot-anim
          working-directory: .
          script: |
            # Wait for emulator to boot
            adb wait-for-device shell getprop sys.boot_completed
            
            # Install the debug APK
            adb install app/build/outputs/apk/debug/app-debug.apk
            
            # Install Maestro CLI
            curl -sL https://get.maestro.mobile.dev | bash
            export PATH="$HOME/.maestro/bin:$PATH"
            
            # Checkout Maestro flows from amakaflow-automation
            cd /tmp
            rm -rf amakaflow-automation
            git clone --depth 1 https://github.com/supergeri/amakaflow-automation.git
            
            # Run Maestro smoke test
            cd amakaflow-automation
            maestro test flows/android/smoke.yaml --env UITEST_AUTH_SECRET="test-secret-123" --env UITEST_USER_ID="ci-test-user"

      - name: Upload Maestro Screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: maestro-screenshots
          path: flows/android/artifacts/screenshots/
          retention-days: 7
          if-no-files-found: ignore

      - name: Determine outcome
        if: always()
        id: outcome
        run: |
          if [ "${{ steps.build.outcome }}" == "failure" ]; then
            echo "ci_outcome=failure" >> "$GITHUB_OUTPUT"
            echo "failed_step=build" >> "$GITHUB_OUTPUT"
          elif [ "${{ steps.tests.outcome }}" == "failure" ]; then
            echo "ci_outcome=failure" >> "$GITHUB_OUTPUT"
            echo "failed_step=test" >> "$GITHUB_OUTPUT"
          elif [ "${{ steps.lint.outcome }}" == "failure" ]; then
            echo "ci_outcome=failure" >> "$GITHUB_OUTPUT"
            echo "failed_step=lint" >> "$GITHUB_OUTPUT"
          elif [ "${{ steps.maestro.outcome }}" == "failure" ]; then
            echo "ci_outcome=failure" >> "$GITHUB_OUTPUT"
            echo "failed_step=maestro" >> "$GITHUB_OUTPUT"
          else
            echo "ci_outcome=success" >> "$GITHUB_OUTPUT"
            echo "failed_step=" >> "$GITHUB_OUTPUT"
          fi

      - name: Generate test summary
        if: always()
        id: summary
        run: |
          SUMMARY=""
          RESULTS_DIR="app/build/test-results/testDebugUnitTest"

          if [ -d "$RESULTS_DIR" ]; then
            TESTS=0
            FAILURES=0
            ERRORS=0
            SKIPPED=0

            for xml in "$RESULTS_DIR"/*.xml; do
              [ -f "$xml" ] || continue
              T=$(grep -oP 'tests="\K[0-9]+' "$xml" | head -1)
              F=$(grep -oP 'failures="\K[0-9]+' "$xml" | head -1)
              E=$(grep -oP 'errors="\K[0-9]+' "$xml" | head -1)
              S=$(grep -oP 'skipped="\K[0-9]+' "$xml" | head -1)
              TESTS=$((TESTS + ${T:-0}))
              FAILURES=$((FAILURES + ${F:-0}))
              ERRORS=$((ERRORS + ${E:-0}))
              SKIPPED=$((SKIPPED + ${S:-0}))
            done

            PASSED=$((TESTS - FAILURES - ERRORS - SKIPPED))
            SUMMARY="**Tests:** ${PASSED} passed, ${FAILURES} failed, ${ERRORS} errors, ${SKIPPED} skipped (${TESTS} total)"
          else
            SUMMARY="No test results found."
          fi

          {
            echo "markdown<<EOF"
            echo "$SUMMARY"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Capture failure logs
        if: always() && steps.outcome.outputs.ci_outcome == 'failure'
        id: failure-logs
        run: |
          LOGS=""
          FAILED="${{ steps.outcome.outputs.failed_step }}"

          if [ "$FAILED" == "build" ]; then
            LOGS="Build failed. Check workflow run for full output."
          elif [ "$FAILED" == "test" ]; then
            RESULTS_DIR="app/build/test-results/testDebugUnitTest"
            if [ -d "$RESULTS_DIR" ]; then
              LOGS=$(grep -h '<failure' "$RESULTS_DIR"/*.xml 2>/dev/null | head -20 | sed 's/<[^>]*>//g' | head -c 2000)
            fi
            if [ -z "$LOGS" ]; then
              LOGS="Unit tests failed. Check workflow run for full output."
            fi
          elif [ "$FAILED" == "lint" ]; then
            LOGS="Lint check failed. Check workflow run for full output."
          elif [ "$FAILED" == "maestro" ]; then
            LOGS="Maestro E2E tests failed. Check workflow run for full output."
          fi

          {
            echo "logs<<EOF"
            echo "$LOGS"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Upload Test Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: agent-test-results
          path: app/build/test-results/
          retention-days: 7
          if-no-files-found: ignore

      - name: Upload Lint Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: agent-lint-results
          path: app/build/reports/lint-results-debug.html
          retention-days: 7
          if-no-files-found: ignore

      - name: Validation Gate
        if: always()
        uses: supergeri/amakaflow-automation/.github/actions/validation-gate@main
        with:
          linear-api-key: ${{ secrets.LINEAR_API_KEY }}
          repo-name: amakaflow-android-app
          ci-outcome: ${{ steps.outcome.outputs.ci_outcome }}
          failed-step: ${{ steps.outcome.outputs.failed_step }}
          failure-logs: ${{ steps.failure-logs.outputs.logs }}
          test-results-summary: ${{ steps.summary.outputs.markdown }}
